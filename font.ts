import { Edu_NSW_ACT_Cursive, Geist } from "next/font/google";

export const cursive = Edu_NSW_ACT_Cursive({
  variable: "--cursive",
  subsets: ["latin"],
  weight: ["400", "500", "600", "700"],
});

export const geist = Geist({
  variable: "--geist",
  subsets: ["latin"],
});
